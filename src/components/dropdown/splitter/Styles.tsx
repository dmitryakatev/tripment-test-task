import { styleWidget } from '@jss-helper';

export const { classes } = styleWidget('drowDownSplitter', {
  dropDownSplitter: {
    width: '100%',
    borderTopWidth: '1px',
    borderTopStyle: 'solid',
    borderTopColor: '#EAEEEE',
  },
});
